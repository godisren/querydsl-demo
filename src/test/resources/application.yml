spring:
  datasource:
    url: jdbc:h2:mem:querydsl
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    jpa:
      properties:
        hibernate:
          format_sql: true